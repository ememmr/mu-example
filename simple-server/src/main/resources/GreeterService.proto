syntax = "proto3";
//TODO: comprobar que este ejemplo está en la nueva version de la docu  que hice
package muServerImpl;

message HelloRequest {
  string arg1 = 1;
  string arg2 = 2;
}

message HelloResponse {
  string arg1 = 1;
  string arg2 = 2;
}

service ProtoGreeter {
  rpc sayHelloProto (HelloRequest) returns (HelloResponse);
}
